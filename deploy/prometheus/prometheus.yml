global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - minxiong-hydrocast.rules.yml

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - 127.0.0.1:9093

scrape_configs:
  - job_name: minxiong-hydrocast
    metrics_path: /metrics
    static_configs:
      - targets:
          - 127.0.0.1:8180
